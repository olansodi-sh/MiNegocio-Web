import ModulePage from '../../../shared/components/ModulePage';
import { Hash } from 'lucide-react';

export default function LotesPage() {
  return (
    <ModulePage icon={<Hash size={28} />} title="Lotes" description="Control por lote" />
  );
}
